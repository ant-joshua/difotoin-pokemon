# Difotoin - Pokemon

## Installation

### Basic Install

1. Clone this repository
2. Run `npm install`
3. Run `npm run dev`

### Production

1. Clone this repository
2. Run `npm install`
3. Run `npm run build`
4. Run `npm run start`
